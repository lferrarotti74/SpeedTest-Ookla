name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@4ffe7f1040213a9743f4fc8fbf8aaa918ff1565f
    with:
      source_branch: main
      target_branch: dev
