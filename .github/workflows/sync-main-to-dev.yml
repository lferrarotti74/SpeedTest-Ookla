name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@2e3c31281cb4cc0e4a5df1c133f1c165e2798ed9
    with:
      source_branch: main
      target_branch: dev
