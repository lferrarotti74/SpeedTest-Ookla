name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@04f57bfd8f50e726c1b74417aec603849eed9f7b
    with:
      source_branch: main
      target_branch: dev
