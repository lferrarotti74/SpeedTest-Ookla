name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@9cd6fd32fc6b6e9677017f541924bd0f88044374
    with:
      source_branch: main
      target_branch: dev
