name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@5d0ddd922da5e921d32c93296bcffabebc0e2c12
    with:
      source_branch: main
      target_branch: dev
