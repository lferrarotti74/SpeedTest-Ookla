name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@bf27820abc7995088eb582674b9f5076fbcb1ae4
    with:
      source_branch: main
      target_branch: dev
