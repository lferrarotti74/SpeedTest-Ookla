name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@74b3365c6a733168c011d4cd523ee579965e2e8b
    with:
      source_branch: main
      target_branch: dev
