name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@d5bd75922374299816ab42e07b62bf7bb239dc87
    with:
      source_branch: main
      target_branch: dev
