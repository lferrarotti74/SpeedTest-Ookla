name: Sync Main to Dev

on:
  push:
    branches:
      - main

permissions:
  contents: write  # needed for pushing commits

jobs:
  sync-main-to-dev:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@490dcf94432c8e39b1b149441fc2cb8bab80ddbe
    with:
      source_branch: main
      target_branch: dev
